# OS / editor
.DS_Store
Thumbs.db
*.log

# local build artifacts (none yet — games are plain static files)
node_modules/
dist/
.tmp/

# keep the workspace file tracked; ignore per-user VSCode state
.vscode/

# dev-only mirror of the platform SDK (tools/dev-serve.sh copies it here so
# /sdk/arcade-sdk.js resolves locally). Never commit the vendor file.
/sdk/
